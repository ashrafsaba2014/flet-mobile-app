app:
  name: "Ashraf Flash Light"
  package: "com.ashraf.flashlight"

build:
  android:
    permissions:
      - CAMERA
    # 💡 هذا الجزء هو الذي يدمج كود الـ Java/Kotlin الخاص بالكشاف
    dependencies:
      - flet_flashlight
      - flet_permission_handler

# الاعتماديات العامة لبايثون
dependencies:
  - flet-flashlight
  - flet-permission-handler
