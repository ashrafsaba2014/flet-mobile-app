app:
  name: "Ashraf Flash"
  package: "com.ashraf.flashlight"

build:
  android:
    permissions:
      - CAMERA
    # 💡 هنا السر: كتابة المكتبات هنا تجبر GitHub على حشر كود الـ Java داخل الـ APK
    requirements:
      - flet-flashlight
      - flet-permission-handler
